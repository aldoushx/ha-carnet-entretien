{
  "config": {
    "step": {
      "user": {
        "title": "CARnet - Garage Log",
        "description": "Free Gemini API key (optional) for automatic generation of the maintenance plan, known issues and resale value estimate. Also choose the content language for the whole integration (card, catalog, AI-generated text, notifications) — this can be changed later from the integration's Options.",
        "data": {
          "gemini_api_key": "Gemini API key (optional)",
          "language": "Content language"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "gemini_api_key": "Gemini API key",
          "language": "Content language"
        }
      }
    }
  }
}
